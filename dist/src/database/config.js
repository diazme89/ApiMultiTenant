"use strict";
//import { dbSynCentral } from '../database/commonDBConnection'; 
//import { initModels } from '../models/models/init-models';
//import { initModelsSynCentral } from '../models/models_syncentral/init-models';
/*export const dbConnection = async() => {

    try {
        await db.authenticate();
        //initModels(db);
        console.log('Se estableció la conexión a la Base de datos.');

    } catch (error) {
        console.log(error);
        throw new Error('Error al inciar la base de datos.');
    }
}*/
/*
export const dbConnectionSynCentral = async() => {

    try {
        await dbSynCentral.authenticate();
        //initModelsSynCentral(dbSynCentral);
        console.log('Se estableció la conexión a la Base de datos Syn Central.');

    } catch (error) {
        console.log(error);
        throw new Error('Error al inciar la base de datos Syn Central.');
    }
}
*/
// export default dbConnection;
//# sourceMappingURL=config.js.map